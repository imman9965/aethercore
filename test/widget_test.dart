// Minimal placeholder so `flutter test` only exercises the concurrency
// harness during architecture verification. Replace with real widget tests
// once the UI lands.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
